package com.company;

public class Main {

    public static void main(String[] args) {

//	      InitializationAndCleanupChapter.Dog.bark();
//	      InitializationAndCleanupChapter.Dog.bark((byte) 10);
//	      InitializationAndCleanupChapter.Dog.bark((short) 10);
//        InitializationAndCleanupChapter.Dog.bark(10);
//	      InitializationAndCleanupChapter.Dog.bark(10L);

//        InitializationAndCleanupChapter.ModifiedDog.bark(true, 2);
//        InitializationAndCleanupChapter.ModifiedDog.bark(2, true);

    }
}
